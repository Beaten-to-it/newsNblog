# 경영 모닝 레이더 — 두 번째 브리핑 트랙 설계

> 작성일: 2026-06-14 · 상태: 설계 승인 대기 → 콘텐츠 검증 스파이크
> 잠정 트랙명: **경영 모닝 레이더** (변경 가능)

## 1. 목표

기존 "AI Morning Radar"와 나란히 도는 **두 번째 일일 트랙**을 추가한다.
- 블로그(GitHub Pages)에 경영 트랙 페이지/섹션을 함께 발행
- "경영 모닝 메일" 한 통을 추가 발송

## 2. 편집 방향 (트랙 정체성)

**박사 연구 주제(AI 도입·조직변화·저항)를 축으로, 경영/비즈니스 뉴스와 MIS/디지털전환을 양념으로 곁들인 "연구 밀착형" 브리핑.**

- AI 트랙(X/Threads/GeekNews 중심의 빠른 개발자·트렌드)과 **성격이 다름**: 더 분석적·근거 중심·느린 호흡.
- 차별점: 사용자의 myOS vault 자산(SLR 코퍼스 1046편, 백본 18편, TAM/UTAUT, 저항 모델)을 **연구 렌즈**로 접목 — 남이 못 만드는 각.

## 3. 소스 전략

층을 섞는다(택일 아님):
- **본문(뼈대):** SNS — **LinkedIn 중심**(경영·실무 본진), X·Threads·공개 웹 보조. 경영 뉴스·기업 AI도입 사례·실무자 여론.
- **연구 렌즈(garnish, 조건부):** vault 개념·논문과 연결한 "왜 중요한가" 심층. **그날 재료가 있으면 넣고 없으면 통째 생략**(억지 padding 금지).

### 열린 질문 / 리스크
- ⚠️ **LinkedIn 접근성:** 로그인·봇차단이 강해 headless `claude -p` 웹검색으로 X/Threads만큼 안 긁힐 수 있음. **스파이크에서 실제 검증 필요.** 부족하면 X·Threads·공개 웹·뉴스레터 아카이브로 무게중심 이동.

## 4. 브리핑 해부도 (= 프롬프트 뼈대)

| # | 섹션 | 내용 |
|---|---|---|
| 1 | 세 줄 요약 | 오늘 경영·AI도입 핵심 3줄 |
| 2 | 실무 담론 레이더 — HR·경영 현장 | SNS(LinkedIn·X·Threads)+HR·경영 트레이드 미디어 화제 담론 3~4개 (어디서 화제 / 무슨 이야기 / 왜 볼 만한가). *§2 이름은 스파이크 결과 반영해 변경(아래 참조)* |
| 3 | 오늘의 핵심 Top 3~5 | 경영·비즈니스 뉴스 + 기업 AI도입·조직변화 사례 (핵심요약 / 왜 중요한가 / 태그) |
| 4 | 🔬 연구 렌즈 (Research Lens) | 오늘 이슈 1~2개를 vault 개념·논문(TAM·저항모델·SLR)과 연결. **재료 없으면 생략.** ← 차별점 |
| 5 | 실무 적용 포인트 | 관리자·조직이 바로 참고할 적용 아이디어 (어디에 / 어떻게 / 주의점) |
| 6 | 체크포인트 | 비즈니스·정책 / MIS·DX 짧은 링크 모음 |
| 7 | 오늘의 관전 포인트 | 내일 이어볼 주제 |

## 5. 롤아웃 — 콘텐츠 먼저 검증 (스파이크)

파이프라인 손대기 전에 **콘텐츠가 매일 잘 나오는지부터** 검증한다(최대 미지수).

1. 위 구조로 `prompts/management_briefing.md` 작성 (AI 프롬프트 미러링)
2. 실제 `claude -p`로 오늘치 테스트 생성 → `briefings/management/{날짜}.md` (AI 트랙과 폴더 분리, 충돌 없음)
3. 함께 평가 — **평가 기준:**
   - LinkedIn/SNS가 실제로 긁히는가
   - 경영 뉴스 분량이 충분한가(매일 Top 3~5 채워지나)
   - 연구 렌즈가 발화하는가(vault 접목이 자연스러운가)
   - 읽을 만한가(AI 트랙과 차별되는 가치가 있나)

스파이크 결과가 좋으면 → 파이프라인 구축 단계로 별도 plan 수립.

## 6. 파이프라인 설계 확정 (2026-06-14)

**트랙명:** AX 모닝 레이더 (AX = AI Transformation). 트랙 키 = `ax`.

기존 AI 트랙은 **경로·URL·이메일 전부 불변**(하위호환). AX만 새 경로 → 충돌 0.

| 요소 | AI 트랙 (기존, 불변) | AX 트랙 (신규) |
|---|---|---|
| 프롬프트 | `prompts/daily_briefing.md` | `prompts/management_briefing.md` |
| 브리핑 폴더 | `briefings/` | `briefings/ax/` |
| 중복방지 DB | `data/published_items.csv` | `data/published_items_ax.csv` |
| 사이트 | `…/newsNblog/` (루트) | `…/newsNblog/ax/` (독립 랜딩·아카이브·브랜드) |
| 글 URL | `…/posts/{날짜}.html` (유지) | `…/ax/posts/{날짜}.html` |
| 이메일 제목 | `{날짜} AI Morning Radar` | `{날짜} AX 모닝 레이더` |
| 수신자 | kimhyo75 + hyoya.kim@samsung | **동일 2인** |

**구현 방침:**
- `render_briefing.py`·`send_email.py`·`build_site.py`를 **track 인자로 파라미터화**. AI는 레거시(비하위경로) 그대로, AX만 하위경로 → 기존 동작 보존 + 코드 재사용.
- `daily_run.py`가 **두 트랙 순차 처리**. AX 리서치는 `claude -p`에 `--add-dir C:\Project\myOS`(vault)로 연구 렌즈 가동. 리서치는 **트랙별 2회 분리**(격리·디버깅 우선; 사용량 부담 시 통합은 후순위 옵션).
- 흐름: (트랙별) 리서치→렌더→이메일 → (공통) 사이트 빌드(둘 다)→1회 push → **트랙별 발송 로그**.
- **발송/캐치업 게이팅을 트랙별로** 분리 — 한 트랙 실패가 다른 트랙을 막지 않게(2026-06-13식 단일 실패점 방지). 발송 로그에 track 컬럼 추가 또는 트랙별 로그.
- 새 예약작업 없음(기존 일일 스케줄·캐치업 재사용). 검증 종료 후 일회성 `newsNblog_MgmtSpike_OneShot` 제거.

**정리 대상(구현 시):** 스파이크 잔여물 — `briefings/management/`(→ `briefings/ax/`로 정리), `scripts/_spike_management_run.py`, `data/_spike_management*.log`.

## 7. 스파이크 결과 (2026-06-14, 2회 실행)

`prompts/management_briefing.md` + `scripts/_spike_management_run.py`(throwaway, vault 읽기 접근 부여)로 오늘치 2회 생성.

**검증 통과 (4기준):**
- 경영 뉴스 분량 🟢 충분 (감원·조직재편·도입 서베이로 매일 채워짐)
- 연구 렌즈 🟢 탁월 — Lapointe & Rivard(2005) 저항모델·TAM 경계를 vault 노트 실제로 열어 연결. **이 트랙만의 차별점 작동 확인**
- 가독성 🟢 좋음
- **SNS 직접 수집 🟡 실패(예상대로)** — LinkedIn·X는 로그인/봇차단으로 직접 안 긁힘 → HR·경영 트레이드 미디어로 정직하게 우회

**1차→2차(프롬프트 수정) 개선 확인:**
- §2 라벨을 "LinkedIn / X / Threads" → **"실무 담론 레이더 — HR·경영 현장"**(현실 반영)
- 출처 티어1 우선 규칙 추가 → content-farm(finalroundai 등) 사라지고 about.gitlab·mckinsey·MIT Sloan 등 1차/티어1로 상승
- 연구 렌즈 **페이지번호 날조 제거** → 노트/citekey 단위 인용으로 교정
- 추가 가드 2건: 희귀 구체값/고유명사 날조 금지, §2 메타(수집 실패) 본문 노출 금지

**남은 검증:** 새 뉴스일(2026-06-15) 1회 더 — "평범한 날"에도 분량·품질 유지되는지.

## 8. 비목표 (YAGNI)

- 스파이크 단계에서 이메일 발송·사이트 빌드·git 발행은 하지 않는다(콘텐츠 생성·평가만).
- LinkedIn 자동 로그인/스크래핑 등 별도 인프라는 만들지 않는다(공개 검색으로 되는 범위만).
